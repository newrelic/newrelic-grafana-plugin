apiVersion: 1

datasources:
  - name: 'newrelic-grafana-plugin'
    type: 'newrelic-grafana-plugin'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    jsonData:
      path: '/resources'
    secureJsonData:
      apiKey: 'api-key'
